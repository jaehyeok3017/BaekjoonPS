#include <stdio.h>
#include <string.h>

int main() {
    int n;
    scanf("%d", &n);

    char a[] = {0, };
    for(int i = 0; i<n; i++){
        scanf("%s", a[i]);
    }

    for(int i = 0; i<n; i++){
        for(int j = 0; j<n; j++){
            // trying 갈아엎을듯
        }
    }
}